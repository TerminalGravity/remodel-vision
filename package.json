{
  "name": "remodel-vision-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @remodel-vision/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  },
  "packageManager": "pnpm@9.15.0"
}

